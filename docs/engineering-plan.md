# 完整产品研发与验收

更新：2026-09-11。此文是后续研发的范围与完成标准，不表示全部能力已实现。

## 产品边界

Mimesis 是运行在用户电脑上的企业级可编排采集网关。用户通过原生浏览器、录制、脚本和可选 Agent 建立可复用流程；业务系统调用固定版本，获得经过校验的数据和明确的失败原因。目标覆盖多种页面与接口模式，不承诺无需配置即可采集任意网站。

“便携”必须落到：可选择的数据目录、相对产物索引、依赖自包含、启动前环境检查、可验证的导入导出、备份恢复和版本升级。换机器时凭据与系统加密绑定如何处理必须明确，不能只复制 Cookie 文件就声称环境完整迁移。Electron Session 隔离不等于操作系统机器码隔离。

“企业级”必须落到：任务与数据一致性、可追踪版本、可解释的恢复行为、资源配额、账号与租约隔离、交付幂等、运行证据、凭据保护、备份、长时间运行及故障注入验收。

## 设计与扩展原则

1. **设计先于交互实现。** 先列清用户任务和所有状态，再生成设计稿与必要素材，再建立真实组件并截图验收。采用顶部导航和单列工作区，不恢复侧栏后台布局。视觉与交互必须符合既定 V3 方向。
2. **一个领域入口。** 桌面、API、Agent 都调用同一套版本发布、任务、环境和数据服务。界面只负责表达状态，不自行推断任务成功。
3. **一个状态权威。** 每个实体明确持久化归属；缓存、UI 状态和事件不是第二个数据库。状态变更与事件/outbox 在同一事务提交。
4. **依赖向内。** contracts → SDK → 执行核心；BrowserHost、存储和模型适配属于外层。网关不控制 WebContents，渲染进程不导入特权模块。依赖图由自动检查约束。
5. **扩展有依据。** 先验证真实场景，再抽取端口。只在具体扩展能力进入开发时建立模块，避免空壳插件系统与重复抽象。
6. **明确副作用。** 失败重试、检查点和恢复必须区分可重放操作与不可重放操作。交付失败不能重新采集；取消不能宣称撤销已经发生的网页行为。
7. **移除替代实现。** 新路径验收后删除旧入口、旧协议、重复样式和无调用模块；历史数据由独立迁移处理并保留备份。

## 功能闭环矩阵

“基础”代表已存在部分可用实现，距离整项完成仍有明确差距。每项完成都需保存代码、测试及操作证据。

| 模块 | 输入与输出 | 完整闭环验收 | 当前状态 |
| --- | --- | --- | --- |
| 浏览器环境 | 环境配置 → 独立浏览器会话 | 页面/标签/弹窗/frame/Shadow DOM、权限、资源释放、人工接管，不串环境 | 基础：Profile 隔离、单页 DOM 与原生操作 |
| 录制与定位 | 用户操作 → 可编辑语义步骤 | 输入、点击、选择、滚动、键盘、上传下载；元素拾取、定位回退、跨页面录制、敏感字段处理 | 基础：普通输入/点击及 CSS 选择器 |
| 编排与脚本 | 参数、流程/源码 → 可发布定义 | 条件、遍历、嵌套循环、子流程、作用域、错误分支、边界预算、受限脚本能力 | 基础：初始化＋一层翻页＋列表→详情遍历（含三层嵌套子遍历）的 JSON 流程、条件执行与失败跳过、受限表达式字段；进程级脚本沙箱与调试断点待实现 |
| 项目与版本 | 编辑版本 → 不可变发布版本 | 校验、试跑、发布、固定输入输出 Schema、内容摘要、回滚、导入导出、调用绑定 | 基础：不可变发布版本、规范化内容摘要校验、发布/回滚、500 版本上限与桌面/网关调用绑定、导入导出、由流程派生的固定输入输出 Schema 与有界试跑；进程级脚本沙箱待实现 |
| 执行与调度 | 固定版本任务 → 明确终态 | 租约、排队、配额、公平调度、超时、取消、检查点、重试策略、崩溃恢复、定时执行 | 基础：单执行器队列与取消 |
| 请求与响应 | HTTP/API 定义或页面网络事件 → 结构化响应 | 请求参数/鉴权、分页游标、超时重试、响应匹配、Schema、浏览器会话关联、体积限制 | 基础：流程内 HTTP 请求动作（参数替换、状态匹配、响应捕获、有界失败重试、超时与体积上限、点击/URL 模板/动态游标三种翻页、可选复用浏览器 Profile 会话 Cookie） |
| 数据管道 | 提取记录 → 校验后数据集 | 类型、字段映射、标准化、过滤、去重键、增量水位、来源信息、坏记录策略 | 基础：字段类型转换、标准化（trim/折叠/大小写）、可配置去重键、记录过滤、坏记录策略、增量水位（含跨运行持久化与清除）、记录来源信息（来源网址/页码/来源类型）、字段映射（交付前重命名输出字段） |
| 存储与产物 | 领域变更/证据 → 事务与受控文件 | SQLite 迁移、索引、事务、outbox、磁盘配额、保留清理、备份恢复、引用完整性 | 基础：独立线程 SQLite、JSON 迁移、分表事务与产物校验索引；归档字节配额、终态保留清理与离线备份恢复；outbox/巡检待实现 |
| 账号与代理 | 账号、环境、代理配置 → 可租用资源 | 密钥保险箱、账号状态、独占租约、轮换、冷却、失效人工处理、代理验证与绑定 | 仅 Profile 基础；其余待实现 |
| Agent | 目标、录制、观察和失败证据 → 建议与验证结果 | 模型配置、能力约束、上下文/费用预算、修复差异、试跑、显式发布、无模型降级 | 待实现；开发 agent 审查不算产品能力 |
| 服务与交付 | 外部请求 → 版本化 API / 可靠交付 | 鉴权轮换、能力权限、契约、幂等、限流、SSE、Webhook outbox、独立交付重试 | 基础：回环 HTTP、Bearer、查询与导出 |
| 可观测性 | 任务事件 → 可定位问题 | 结构化日志、步骤证据、错误分类、指标、环境/队列/磁盘健康、脱敏导出 | 基础：Run 与 Step 状态 |
| 便携与发行 | 发布包/旧工作区 → 可运行可升级安装 | 干净 Windows 验证、移动目录、中文路径、只读目录处理、开机/托盘、防休眠、退出清理、升级恢复 | 基础：设置页自定义目录、下一次启动复制校验、中文路径/Cookie/归档恢复；完整发行待验收 |
| 设计系统 | 使用场景和状态 → 一致可操作界面 | 设计稿→真实组件→最小窗口/键盘/双语/错误状态/可用性验收 | V3 基础；完整交互体系仍需逐模块设计 |

## 依赖顺序

不删减上述范围，按能被真实验证的纵向功能分批完成：

1. **统一底座**：清除伪功能遗留、规范依赖、领域拆分、事务存储与数据迁移、产物索引和文件配额。
2. **完整编排**：版本模型、递归但有界的流程解释器、脚本能力代理、调试/断点和运行证据；桌面和 API 调用同一发布版本。
3. **采集与数据能力**：网络请求/响应、复杂页面、列表详情循环、数据 Schema、清洗管道、增量采集和可靠交付。
4. **生产资源管理**：账号、代理、密钥、环境租约、并发配额、恢复与定时调度。
5. **Agent 闭环**：复用前述观察/执行端口，生成或修复候选流程，显示差异，经过验证后发布；禁止静默修改生产版本。
6. **便携发行与综合验收**：覆盖全流程的设计审查、干净机器安装/迁移、更新回滚、故障注入、压力与持续运行。

每批提交须同时覆盖正常路径与相关失败路径，不以“接口已写”“页面已有”“效果图已画”标记模块完成。已有架构提案用于背景参考，冲突时以本文件、当前契约和用户指令为准。

## 关键技术决策进入实现前必须验证

- SQLite 在 Electron/Node 中的运行兼容性、事务边界与非阻塞访问方式；不能把现有整个 JSON 当作一行 BLOB 就称为事务架构完成。
- 受限脚本的进程/运行时隔离、终止能力和权限代理。Node vm 本身不作为不可信代码安全沙盒。
- 浏览器控制权、取消后的操作清理、租约 fencing 与人工接管。
- 代理/账号切换与正在运行任务的不可变绑定。
- 模型服务配置、敏感信息处理和能力调用审计；模型供应商通过端口隔离。
- 便携数据、系统加密凭据及跨机器导出的冲突处理。

## 生产验收门槛

- 用标准表单、多层列表详情、动态 SPA、登录会话、API 游标分页等不同模式验证可编排能力。
- 验证网络中断、页面超时、进程崩溃、磁盘写满、账号失效、重复请求、交付失败和升级失败；结果与恢复语义符合契约。
- 在已公布资源配置下测量成功率、延迟、内存/磁盘增长和队列积压；未经测量不写容量承诺。
- 进行至少 72 小时连续运行及发行前更长周期验证，检查文件保留、租约释放、恢复、数据一致性。长期测试不能用快速单测替代。
- 干净 Windows 环境验证运行包，移动路径验证正常，升级和备份恢复可重复执行。
- 完成中英双语、键盘可用性、1080×760 最小窗口及真实操作截图检查，无死按钮、虚构指标或覆盖页面的面板。

## 本批落实记录

2026-09-11 第一批：移除未执行的旧草稿组件、公开协议与 sample-source 出口；工作区使用 schema v3，历史格式隔离于 migrations，原始数据先保留版本备份；网关与工作区复用同一套原子文件写入；新增依赖图、声明依赖和跨包导入检查。该批仍使用有界 JSON 存储。

本批验证：pnpm check 通过，57 项单元测试通过；本地录制/循环/API 导出、网关恢复通过 Electron 回归；旧 schema v2 工作区备份与升级、Profile 隔离、重启持久化和双语界面通过专项 Electron 回归。外部 Quotes 测试本批未重跑，保留 2026-09-09 的既有验收记录。首轮 Profile 测试暴露了读取异步切换结果过早的问题，已改为等待实际环境切换完成后验证。

2026-09-11 第二批：以专用工作线程持有 SQLite，桌面与网关共享 profiles/instances/runs/steps/jobs/artifacts 分表数据；终态和产物索引事务提交；一次性导入旧 JSON 并保留原文备份；移除旧 JSON 写入仓储；退出时等待桌面及网关任务取消保存。[存储决策与边界](runtime-storage.md)明确保留当前队列容量限制，不将数据库接入等同于企业级功能全部完成。

第二批验证：`pnpm check` 通过，67 项测试覆盖迁移备份、数据库事务回滚、版本/损坏拒绝、共享运行归属、线程异常退出和产物索引；真实 Electron 验证录制循环/API 导出、迁移重启/Profile 隔离与双语界面、网关退出恢复及四种归档文件 SHA-256、无网关时桌面任务取消落盘。外部站点采集、长时间压力和打包发行本批未重新验收。

2026-09-11 第三批：先生成存储设置设计图，接入自定义数据目录、保存/撤销/打开/原生选择目录；启动配置与数据根目录分离，userData/sessionData 同步绑定。下次启动在会话初始化前完成复制、SHA-256 校验和配置切换，保留原目录；配置损坏或目标占用不回退空工作区。开发启动器尊重显式环境覆盖和已保存设置。

第三批验证：`pnpm check` 与 76 项测试通过；5 条 Electron 场景覆盖自定义目录迁移、中文路径、Cookie/语言/SQLite/API 归档/幂等键保留，以及既有录制循环、Profile 隔离、网关恢复、退出取消。设计及实际中英/最小窗口截图见 docs/design/storage-v1。磁盘配额、保留清理、大目录进度、跨卷/干净机器发行和完整备份恢复仍待实现或验收。

2026-09-12 第四批：网关归档实施字节预算（默认 2 GiB）与历史条数（默认 1000）双重上限，超出后从最旧终态任务开始清理，未完成任务与队列顺序不可变；产物清单跨重启持久化，清理失败时数据库不声称已删除。设置页新增离线备份/恢复：在启动引导期（任何数据库句柄与会话创建前）执行清单→暂存复制→逐文件 SHA-256 校验→回执→原子改名，原目录永不修改；恢复只接受完整回执的备份，目标为新目录而非覆盖现有工作区；与目录迁移互斥，每次启动至多一次重传输。

第四批验证：备份/恢复单元测试覆盖空目标校验、嵌套/符号链接拒绝、逐文件校验失败、恢复后损坏工作区被替换、回执与暂存清理；网关测试覆盖配额驱逐、跨重启继续清理与清理失败 fail-closed。Electron 场景验证损坏工作区经备份恢复后可正常启动。大目录进度、跨卷迁移、非 Windows 发行与长期压力本批未重新验收。

2026-09-14 第五批：新增不可变发布版本。发布前校验流程并生成规范化内容摘要（目标地址 + 规范化流程），版本一经发布不再修改；同一实例内重复发布相同内容复用原版本号，不产生重复版本，达到 500 个版本时以 `VERSION_LIMIT` 拒绝新增且不自动清理历史。运行只使用绑定版本：未发布内容拒绝执行（`NO_PUBLISHED_VERSION`），存储内容与摘要不一致时拒绝执行（`VERSION_CONFLICT`），编辑草稿不影响已冻结内容；回滚只改变“运行什么”，草稿、目标地址与备注保持不变；已受理的桌面运行与网关任务保留受理时的版本快照。每次运行记录保存本次执行的版本标识，历史运行可回溯到发布版本。新增“版本”页（中英双语），桌面运行与网关 job 通过 `versions.publish` / `versions.rollback` 复用同一套领域逻辑。

第五批验证：`pnpm check` 通过，18 个测试文件 99 项测试通过（含 script-registry 版本模型 9 项、storage 运行表版本列迁移与旧库升级）；Electron 回归 `versions.spec.ts` 覆盖未发布拒绝运行、重复发布去重、草稿改动不改冻结内容、重新发布换绑、回滚、网关任务绑定版本、中英版本页与列表切换换绑。试跑、固定输入输出 Schema、版本导入导出尚未实现；长时压力、真实外网与发行验收本批未重跑。

2026-09-14 第六批：编排解释器支持条件与错误分支，运行证据可按步定位。初始化为每个操作增加 `when.exists` 执行条件：元素不存在时记为“已跳过”，不再当作失败；并可把可恢复的操作失败降级为跳过，取消与运行级超时仍终止本次运行。步骤新增 `skipped` 状态，跳过步骤保留选择器/网址与原因，运行可诊断为“成功但含跳过步骤”，不与成功或失败混同。浏览器能力端口将 `hasNext` 统一为 `exists`，同一预设检查同时服务分页与条件。桌面流程编辑器提供条件输入与“失败时跳过”开关（中英双语），运行结果以中性色和减号图标区分跳过步骤。

第六批验证：`pnpm check` 通过（含类型检查、Lint、构建与架构/i18n 约定检查），18 个测试文件 104 项测试通过，其中解释器用例覆盖“条件不满足记为跳过”和“失败降级为跳过且仍完成本页”，运行器用例覆盖 `attempt` 记录跳过并保留错误原因。嵌套子流程、遍历与作用域、受限脚本沙箱、运行中人工接管、真实外网与发行验收本批未实现或未重跑。

2026-09-14 第七批：实现列表→详情遍历并修复其真实浏览器缺陷。流程新增 `detail` 配置：每页列表提取行字段后，按快照的行元素逐行点击行内链接打开详情页，提取详情字段合并进对应行记录，再返回列表继续；返回优先使用真实浏览器历史，无历史时回退到配置的返回控件。每页遍历条数上限 1–500（默认 10），超出标记 `truncated`；浏览器能力端口新增 `snapshotItems`/`actOnItem`（按绝对路径把操作限定在快照行内）。本批修复上一批遗留：`snapshotItemPaths` 经序列化注入页面却引用模块作用域函数，真实浏览器中必然 `INVALID_INPUT`——已改为自包含实现；同时清理遗留 Lint 错误、三元表达式与死代码。

第七批验证：`pnpm check` 通过（含类型检查、Lint、构建与架构/i18n 约定检查），18 个测试文件 110 项测试通过，新增解释器用例覆盖逐行遍历合并字段、`maxItems` 截断、无历史时返回控件回退，端口用例覆盖快照作用域定位与越界索引 `NOT_FOUND`；新增 Electron 回归 `detail-traversal.spec.ts` 在真实 Chromium 中验证列表页→两个详情页→历史返回→回到列表的完整链路及运行证据（本地 HTTP 站点）。全套 9 条 Electron 回归 8 通过 1 跳过（真实外网站点用例）。嵌套子流程、作用域与受限脚本沙箱仍待实现；真实外网与发行验收本批未重跑。

2026-09-15 第八批：编排解释器支持嵌套子遍历（作用域延续）。`detail` 节点新增 `rows`（详情页内嵌套列表提取）与 `children`（对每个嵌套行继续应用的子遍历），整个遍历递归但有界：Schema 校验层拒绝深度超过 3 的配置，运行时嵌套行先按共享去重集合与记录/字节预算落入数据集，再逐行打开子详情页合并字段后返回；嵌套行使用各自的快照作用域，不与外层行索引混淆。已发布旧版本内容（无 `rows`/`children`）完全兼容。桌面流程编辑器改为递归 `TraversalEditor`，提供嵌套列表与子遍历开关（中英双语，UI 同样限定三层）。

2026-09-15 第九批：实现受限表达式计算字段（受限脚本能力第一片）。提取字段新增可选 `expression`：自建有界 tokenizer/Pratt parser/求值器（`packages/script-registry/src/expression.ts`），不使用 `eval`/`new Function`/Node vm；语法仅含字符串/数字/布尔字面量、同记录其他字段引用 `{name}`、10 个白名单函数（upper/lower/trim/length/replace/substring/concat/number/round）与固定运算符集，无属性访问、无循环；源长度 ≤1000、AST 节点 ≤200、函数实参 ≤8 封顶，解析结果带缓存。求值发生在页面提取之后（列表、详情合并、嵌套列表三个提取点统一接入），失败按步骤失败暴露。桌面字段编辑器新增表达式输入（中英双语，提示白名单函数与失败语义）。

第九批验证：`pnpm check` 通过（类型、Lint、构建、架构/i18n 约定），19 个测试文件 117 项测试通过；新增 `expression.test.ts` 覆盖字符串/算术/比较/逻辑运算与优先级、未知函数/属性访问/构造器标识/超深表达式/缺字段拒绝；collection 集成用例覆盖表达式字段进入记录与坏表达式导致步骤失败。进程级脚本沙箱（不可信代码）、调试断点与运行证据回放仍待实现；真实外网与发行验收本批未重跑。

2026-09-15 第十批：实现版本导入导出（“项目与版本”闭环补齐导入导出片）。任一已发布版本可导出为单版本便携文件（`mimesis-version`，含目标地址、流程内容、备注与内容摘要，导出前先验证存储内容与摘要一致）；导入时用 Schema 严格解析文件、重算内容摘要比对，不匹配即以 `VERSION_CONFLICT` 拒绝，解析失败 `INVALID_INPUT`，文件体积封顶 512 KiB。导入通过校验后按目标实例现有版本续号发布为新版本（内容相同复用原版本号、计入 500 上限），且不改变当前运行绑定——是否切换仍由用户在版本列表显式操作。文件对话框与读写只存在于主进程（导出拒绝覆盖已有文件），渲染进程仅获得结果；中英双语提示覆盖导出取消、导入成功与文件无效三类反馈。

第十批验证：`pnpm check` 通过（类型、Lint、构建、架构/i18n 约定），19 个测试文件 121 项测试通过；versions 测试新增 4 项覆盖导出→导入往返、导入续号、篡改摘要拒绝、坏文件与损坏版本拒绝导出。跨实例导入的 Electron 端到端回归、真实外网与发行验收本批未重跑。

2026-09-15 第十一批：实现固定输入输出 Schema 与试跑（“项目与版本”闭环补齐最后两片）。`planWorkflow` 由不可变流程内容确定性派生：输入参数名（首现顺序）、输出字段（列表字段→详情字段→嵌套行字段，跨提取点重名拒绝）、页数/记录/遍历上限；计划与内容摘要一一对应，内容变则 Schema 变，无需独立存储。试跑 `runs.dry` 对草稿流程执行有界单页验证：分页关闭、maxRecords 封顶 20、遍历 maxItems 封顶 3，参数仍需齐备；走独立 dry runner（60 秒超时），事件仅推送渲染进程，不写入运行历史库，不破坏“运行只绑定已发布版本”不变量。桌面流程页新增“输入输出 Schema”与“试跑”按钮（中英双语）。

第十一批验证：`pnpm check` 通过（类型、Lint、构建、架构/i18n 约定），19 个测试文件 124 项测试通过；collection 测试新增 3 项覆盖计划派生（含嵌套遍历字段顺序）、重名输出字段拒绝、试跑变体封顶且不改源流程。至此“项目与版本”模块全部闭环；试跑的 Electron 端到端回归、真实外网与发行验收本批未重跑。

2026-09-15 第十二批：实现“请求与响应”模块第一片——流程初始化动作新增 HTTP `request`。契约层新增 `httpRequestSchema`（方法 GET/POST/PUT/PATCH/DELETE、URL/头部/正文均支持 `{{参数}}` 替换、超时 100ms–30s、`expectStatus` 状态匹配、可选 `capture` 捕获响应体 ≤64 KiB）与 `REQUEST_FAILED` 错误码、`request` 步骤类型；`script-sdk` 新增 `HttpPort` 端口（脚本只描述请求，网络只经宿主执行）；`browser-host` 实现 `HttpHost`（Electron `net`，仅 http/https、拒绝带凭据 URL、超时中止、超体积即拒）；执行链路：request 在 before 序列中按序执行，响应体捕获后可被后续 fill 以 `{{response:name}}` 引用（发布校验拒绝引用未捕获名与重名捕获），状态不匹配以 `REQUEST_FAILED` 失败，支持 `when.exists` 条件与 `onError: skip`；旧已发布版本（无 request 动作）完全兼容。桌面流程编辑器新增 HTTP 请求动作（方法/地址/捕获名输入，中英双语）。

第十二批验证：`pnpm check` 通过（类型、Lint、构建、架构/i18n 约定），19 个测试文件 127 项测试通过；collection 测试新增 3 项覆盖请求执行与捕获注入 fill、状态不匹配失败、未捕获引用拒绝。请求动作的 Electron 端到端回归（真实 Chromium + 本地 HTTP 服务）、浏览器会话关联、分页游标与真实外网验收本批未重跑。

2026-09-15 第十三批：实现“数据管道”第一片——可配置去重键。流程新增 `dedupe` 字段名数组（默认空＝整行去重）：发布校验要求每个名字都是计划输出的真实字段（含详情/嵌套遍历字段）且不得重复，未知名字以 `INVALID_INPUT` 拒绝；运行时列表行与嵌套行的去重键统一改为“列出字段值的有序序列”，记录不含任何列出字段时回退整行键（保持跨提取点行为可预期）；字节预算改按完整记录计。新增 `script-registry/fields.ts` 承载字段名与去重键工具，避免 collection 与 versions 循环依赖。桌面流程编辑器“循环”区新增去重字段输入（中英双语，逗号分隔、上限 8 个）。已发布旧版本内容（无 `dedupe`）经 Schema 默认值完全兼容，内容摘要因规范化包含 `dedupe: []` 不变。

第十三批验证：`pnpm check` 通过（类型、Lint、构建、架构/i18n 约定），19 个测试文件 130 项测试通过；新增用例覆盖跨页按字段去重（第二页同键不同附加字段的行被丢弃）、整行回退与空配置、发布校验接受已知字段并拒绝未知/重复名字。字段映射、类型转换、标准化、过滤、增量水位与坏记录策略仍待实现；Electron 端到端与真实外网验收本批未重跑。

2026-09-15 第十四批：实现“数据管道”记录过滤。流程新增可选 `filter`：一条受限表达式（复用表达式引擎，仅字段引用/白名单函数/固定运算符，长度 ≤1000），在每条记录入集之前求值（列表行与嵌套行统一），结果为假（空串/false/0）的记录被丢弃，不占去重与预算；解析失败按步骤失败暴露。发布校验要求表达式可解析且只引用计划输出字段，未知字段以 `INVALID_INPUT` 拒绝。三个入集点（列表行、子遍历前嵌套行、无子遍历嵌套行）统一为共享的 admitRecord 门（过滤→去重→预算），消除重复逻辑。桌面流程编辑器“循环”区新增过滤表达式输入（中英双语，附语义提示）。已发布旧版本内容（无 `filter`）经 Schema 默认值完全兼容；内容摘要因规范化省略 `undefined` 字段不变。

第十四批验证：`pnpm check` 通过（类型、Lint、构建、架构/i18n 约定），19 个测试文件 132 项测试通过；collection 新增用例覆盖假值记录被丢弃（含空串）、发布校验接受已知字段并拒绝未知字段与坏语法。字段类型转换、标准化（trim/大小写/单位）、增量水位与坏记录策略仍待实现；Electron 端到端与真实外网验收本批未重跑。

2026-09-15 第十五批：实现“数据管道”字段标准化。提取字段新增可选 `normalize`（trim 去首尾空白 / collapse 去首尾并把连续空白折叠为单个空格 / upper / lower，缺省＝不处理），在三个提取点（列表行、详情合并、嵌套行）统一于表达式计算之前应用——表达式与过滤/去重看到的都是规范化后的值。使用 optional 不带默认值，旧已发布版本内容摘要不变；canonical 投影省略 `undefined`，编辑器关闭标准化时同样移除该键。桌面字段编辑器新增标准化下拉列（中英双语，附语义提示）。

第十五批验证：`pnpm check` 通过（类型、Lint、构建、架构/i18n 约定），19 个测试文件 133 项测试通过；新增用例覆盖 collapse/upper 规范化先于表达式求值（concat 看到清洗后的输入）。字段类型转换、增量水位与坏记录策略仍待实现；Electron 端到端与真实外网验收本批未重跑。

2026-09-15 第十六批：实现“数据管道”坏记录策略。提取配置（列表、详情、嵌套行三个提取点统一）新增可选 `missing: "page" | "row"`：缺省与 `page` 保持历史语义——任一行必填字段为空时该页提取结果整体为空，轮询可在异步字段加载后重试；`row` 时页面内不再整页清空，必填缺失的记录带回宿主侧，由共享的 `dropIncompleteRows` 门统一丢弃，表达式/过滤/去重看到的都是完整行。`missing` 为 optional 无默认值且 canonical 投影省略 `undefined`，旧已发布版本内容摘要不变；编辑器选择“整页（默认）”时同样移除该键。桌面流程编辑器三个提取点均提供坏记录策略下拉（中英双语，附语义提示）。

第十六批验证：`pnpm check` 通过（类型、Lint、构建、架构/i18n 约定），19 个测试文件 136 项测试通过；browser-host 用例验证 `row` 策略下不完整记录透传回宿主，collection 集成用例验证宿主侧只丢弃不完整行且策略随提取输入到达浏览器端口，发布校验用例验证 `missing` 可解析、非法值拒绝、旧内容摘要不变。页面内整页清空分支运行在注入页面的序列化函数中，单测 mock 无法触达，由既有 Electron 回归覆盖；字段类型转换与增量水位仍待实现；Electron 端到端与真实外网验收本批未重跑。

2026-09-15 第十七批：实现“数据管道”字段类型转换。提取字段新增可选 `type`（string 缺省 / number / boolean）：在标准化与表达式计算之后、记录进入数据集之前统一由共享 `applyTypes` 门转换（列表行、详情合并、嵌套行三个提取点共用）；number 要求有限数字解析（空串失败），boolean 接受 true/false（大小写不敏感）与 1/0 字面量，失败按步骤失败 `INVALID_INPUT` 暴露，静默不转换。`type` 为 optional 无默认值且 canonical 投影省略 `undefined`，旧已发布版本内容摘要不变；编辑器选择“字符串（默认）”时同样移除该键。桌面字段编辑器新增输出类型下拉（中英双语，附语义提示）。

第十七批验证：`pnpm check` 通过（类型、Lint、构建、架构/i18n 约定），19 个测试文件 138 项测试通过；collection 新增用例覆盖类型转换成功、非法数字/布尔失败步骤失败、旧内容摘要不变。增量水位仍待实现；Electron 端到端与真实外网验收本批未重跑。

2026-09-15 第十八批：实现“数据管道”增量水位。流程新增可选 `watermark: { field }`：发布校验要求字段为计划输出字段（未知名 `INVALID_INPUT` 拒绝）；运行输入新增可选上次水位值，三个入集点（列表行、子遍历前嵌套行、无子遍历嵌套行）在过滤/去重/预算之前先做水位比较——双方可解析为有限数字则按数值比较，否则按字符串比较，相等与不超过的记录直接丢弃且不占预算；每页零新增在配置水位时以新终态 `watermark-reached` 停止（区别于 `no-new-records`），快照 `collection.watermark` 返回本次最高水位值供下次运行使用。`watermark` 为 optional 无默认值且 canonical 投影省略 `undefined`，旧已发布版本内容摘要不变。桌面流程编辑器“循环”区新增增量水位字段输入（中英双语，附比较语义提示）。

第十八批验证：`pnpm check` 通过（类型、Lint、构建、架构/i18n 约定），19 个测试文件 141 项测试通过；collection 新增用例覆盖数值水位过滤＋新最高水位返回、整页无超过记录时 `watermark-reached`、发布校验接受已知字段并拒绝未知名。水位的跨运行持久化由调用方（实例状态/网关 job）串联仍待实现；Electron 端到端与真实外网验收本批未重跑。

2026-09-15 第十九批：串联增量水位的跨运行持久化，补齐“数据管道”增量采集闭环。实例状态新增可选 `lastWatermark`（契约 `automationInstanceSchema.lastWatermark`，optional 无默认值）；工作区 schema 升到 v5，`instances` 表新增 `lastWatermark TEXT` 列并附迁移，历史 v1–v4 工作区经 `decodeState` 升级时该字段缺省即视为“未记录”，已发布旧版本内容摘要不受影响。桌面运行：成功运行若返回 `collection.watermark` 则写回对应实例 `lastWatermark` 并持久化；`runs.start` 把当前水位作为运行输入传入，运行器复用既有 watermark 比较。网关：`GatewayExecution` 新增可选 `watermark`，`resolve` 在受理时快照当时实例水位，`execute` 把它传给运行器，使排队任务按受理时的稳定值执行、不受后续跑动影响。桌面新增 `instances.watermark.clear` 主进程方法（预加载暴露 `clearWatermark`），清除后下一次运行从头采集；实例配置页在有水位流程时显示上次水位值并提供“清除水位”按钮（中英双语），无可清除值时给出“尚未记录”提示。

第十九批验证：`pnpm check` 通过（类型、Lint、构建、架构/i18n 约定），19 个测试文件 142 项测试通过；runtime-database 新增用例覆盖水位往返与“清除后回落为未设置（非字符串 null）”；既有旧库升级用例同步移除新列以继续模拟 v1 数据库语义。水位的 Electron 端到端回归（真实 Chromium + 本地站点跑两轮验证跨运行续采）与真实外网验收本批未重跑。

2026-09-15 第二十批：实现“数据管道”记录来源信息。流程新增可选 `source: { url?, page?, origin? }`（`strictObject` 且要求至少启用一项）：启用后每条记录附带保留字段 `sourceUrl`（来源列表页地址）、`sourcePage`（1 基页码）与 `sourceOrigin`（`list` | `nested`）。来源字段在记录准入之前注入，因此过滤/去重/水位都能引用；子遍历产生的嵌套行沿用其所属列表页的网址与页码，来源类型记为 `nested`（子遍历逐层共用同一 `decoratePage` 回调）。发布校验要求保留字段名不得与任一提取字段（含详情/嵌套行字段）重名，`planWorkflow` 输出按“列表字段→详情字段→来源字段（url/page/origin 固定顺序）”排列，来源名同时纳入 `outputFieldNames` 供去重/过滤/水位引用校验。`source` 为 optional 无默认值且 canonical 投影省略 `undefined`，未启用的旧已发布版本内容摘要不变。桌面流程编辑器“循环”区新增来源网址/页码/来源类型三个勾选项（中英双语，附语义与命名冲突提示），全部取消时移除 `source` 键而非留空对象。

第二十批验证：`pnpm check` 通过（类型、Lint、构建、架构/i18n 约定），19 个测试文件 144 项测试通过；collection 新增用例覆盖列表记录附带网址/页码/来源类型且计划输出含来源字段、保留名与提取字段冲突被拒、启用来源改变内容摘要。字段映射仍待实现；来源信息的 Electron 端到端与真实外网验收本批未重跑。

2026-09-15 第二十一批：实现“数据管道”字段映射，补齐该模块最后一片。流程新增可选 `mapping: [{ from, to }]`（`to` 拒绝保留标识 `constructor`/`prototype`，上限 64 条）：在记录交付前重命名输出字段键（`fields.ts` 新增 `orderedFieldNames`/`mappingTarget`/`mappedOutputNames`，`outputFieldNames` 复用有序版本）；映射在过滤/去重/水位之后应用，因此这三者仍以源字段名表达。发布校验：`from` 必须是计划输出的真实字段（含详情/嵌套遍历与来源字段）且不得重复，重命名后的输出名必须唯一——重复 `to`、与未映射字段名冲突或映射到互为对象的保留名均以 `INVALID_INPUT` 拒绝；`planWorkflow` 输出顺序变为“列表→详情→来源”后套用映射。`mapping` 为 optional 无默认值且 canonical 投影省略 `undefined`，未配置映射的旧已发布版本内容摘要不变；桌面流程编辑器“循环”区新增字段映射行（源字段/输出名，可增删，中英双语，全部删除时移除键而非留空数组）。

第二十一批验证：`pnpm check` 通过（类型、Lint、构建、架构/i18n 约定），19 个测试文件 147 项测试通过；collection 新增用例覆盖交付重命名且去重仍按源字段名、`planWorkflow` 输出套用映射、发布校验接受已知源字段并拒绝未知/重复源字段/重复目标/与未映射字段冲突、配置映射改变内容摘要。字段映射的 Electron 端到端与真实外网验收本批未重跑。

2026-09-15 第二十二批：实现“请求与响应”有界失败重试。`httpRequestSchema` 新增可选 `retries`（0–5，缺省不重试）与 `retryDelayMs`（0–10000，缺省 500ms 退避）；网络错误、超时与状态不匹配均触发重放，用尽次数后仍以 `REQUEST_FAILED`/原错误终止本次步骤，取消（`signal`）立即中止重试循环而非继续重放。重试前的捕获名替换只计算一次（URL/头部/正文），失败尝试不会写入捕获值——只有最终成功的一次才写入 `captures`，因此后续 `{{response:name}}` 引用与重试语义一致。`retries`/`retryDelayMs` 均为 optional 无默认值且 canonical 投影省略 `undefined`，未配置重试的旧已发布版本内容摘要不变；解释器新增通用 `wait(signal, ms)` 并让既有 `pause` 复用它。桌面流程编辑器 HTTP 请求动作新增重试次数与重试间隔输入（中英双语，次数为 0 或间隔等于默认值 500 时移除对应键而非留空）。
2026-09-15 第二十二批验证：`pnpm check` 通过（类型、Lint、构建、架构/i18n 约定），19 个测试文件 149 项测试通过；collection 新增用例覆盖“一次瞬时失败后重试成功（共 3 次调用并正常出数据）”与“用尽重试次数后以 `REQUEST_FAILED` 失败且恰好调用 2 次”。重试的 Electron 端到端回归（真实 Chromium + 本地 HTTP 服务）与真实外网验收本批未重跑。

2026-09-16 第二十三批：实现“请求与响应”分页游标第一片——URL 模板翻页。`pagination` 变为两种互斥模式：既有点击模式（`{ next, maxPages }`）与新增页码网址模式（`{ urlTemplate, startPage, maxPages }`，Schema 强制模板包含 `{{page}}`，`startPage` 缺省 1 且为 0–1000 整数）。解释器在 URL 模式下不再查找下一页按钮：达到 maxPages 以 `page-limit` 停止，否则把 `startPage + 已收页数` 代入 `{{page}}`、经运行参数（resolve 阶段烘焙）与 `{{response:name}}` 捕获（运行时替换）生成目标地址，以 `navigate` 步骤进入下一页并保留既有“URL 变化的重复页”检测语义。`parameterNames` 统一收编 fill 与 urlTemplate 的占位符解析：`{{page}}` 视为分页游标不算运行参数，模板引用未捕获响应名或残留散落大括号以 `INVALID_INPUT` 拒绝；运行参数缺失在 resolve 阶段即拒绝。旧已发布点击模式版本完全兼容（union 分支缺省不变）。桌面流程编辑器“循环”区新增翻页方式下拉（点击/页码网址）与模板、起始页码输入（中英双语），两种模式互切保留 maxPages。

第二十三批验证：`pnpm check` 通过（类型、Lint、构建、架构/i18n 约定），19 个测试文件 151 项测试通过；collection 新增用例覆盖 URL 模板翻页（参数烘焙＋游标代入＋maxPages 截断＋无点击动作＋navigate 步骤证据）与模板占位符校验（未知捕获引用/散落大括号拒绝、`{{page}}` 不进参数表、合法模板发布校验通过）；versions 用例补 URL 模板下参数名收集。2026-09-16 补：新增 Electron 回归 `url-pagination.spec.ts`，在真实 Chromium + 本地三页 HTTP 站点验证游标逐页代入（记录按页序合并）、maxPages 以 `page-limit` 截断、运行结束于末页、步骤证据仅含 navigate（首页＋两次游标页）且无任何分页点击；全套 Electron 回归 10 通过 1 跳过（真实外网站点用例）。

2026-09-16 第二十五批：实现“请求与响应”动态游标（分页游标第二片）。`pagination` 新增第三种互斥模式 `{ cursor: { request, pattern }, maxPages }`：每页提取完成后执行 `request`（复用 `httpRequestSchema`，含重试/超时/状态匹配），用 `pattern`（合法正则，发布时校验）在响应体上取首个捕获组（无捕获组取整体匹配）作为下一页地址，以 navigate 步骤进入；无匹配以新终态 `cursor-exhausted` 停止（不标记 truncated），达到 maxPages 先停、不发多余请求。运行参数在 resolve 阶段烘焙进游标请求（`bakeRequest`，before 动作与游标请求共用），`{{response:name}}` 捕获在执行时替换；`parameterNames` 把游标请求的 URL/头部/正文占位符纳入参数收集与校验。旧已发布点击/URL 模式版本完全兼容（union 分支缺省不变）。桌面流程编辑器翻页方式下拉新增“动态游标”，提供游标请求网址与下一页正则输入（中英双语）。解释器把 before 序列与游标请求的执行统一为共享 `fetchWithRetry`，消除重复重试逻辑。

第二十五批验证：`pnpm check` 通过（类型、Lint、构建、架构/i18n 约定），19 个测试文件 153 项测试通过；collection 新增用例覆盖“动态游标翻页（两次 request 步骤、按响应体地址 navigate、耗尽以 `cursor-exhausted` 停止、无点击动作）”与“达到 maxPages 先停且只发一次游标请求”。游标翻页的 Electron 端到端回归（真实 Chromium + 本地 API 站点）、浏览器会话关联与真实外网验收本批未重跑。

2026-09-16 补：新增 Electron 回归 `cursor-pagination.spec.ts`，在真实 Chromium + 本地三页站点与游标 API 上验证：游标响应体地址逐页 navigate（记录按页序合并，每页一次 request 步骤、无任何分页点击）、耗尽以 `cursor-exhausted` 停止且不标记 truncated、maxPages 以 `page-limit` 先停且不多发游标请求。全套 Electron 回归 12 通过 1 跳过（真实外网站点用例）。浏览器会话关联与真实外网验收仍未重跑。

2026-09-16 第二十六批：实现“请求与响应”浏览器会话关联。`httpRequestSchema` 新增可选 `useSession`：启用后该请求从当前浏览器 Profile 会话发出（Electron `net.request` 绑定 Profile session），携带环境 Cookie 与会话状态，实现登录态 API 采集；缺省仍为隔离请求，两者语义与既有内容摘要完全兼容（optional 无默认值，canonical 投影省略 `undefined`，取消勾选即移除键）。链路：契约 → `HttpPort.fetch` 请求对象新增 `useSession` 标志 → 解释器 `fetchWithRetry` 透传 → `HttpHost` 构造时注入会话提供者（`BrowserHost.currentSession()` 返回当前选中 Profile 的 session）→ `net.request({ session })`。游标请求与 before 动作统一经 `fetchWithRetry`，同样支持。桌面流程编辑器 HTTP 请求动作新增“复用浏览器会话”开关（中英双语），提示携带当前环境 Cookie。

2026-09-16 第二十六批验证：`pnpm check` 通过（类型、Lint、构建与架构/i18n 约定），19 个测试文件 155 项测试通过；collection 新增用例覆盖 `useSession` 透传到 HTTP 端口、未启用时不出现该标志（保持既有摘要）。2026-09-16 补：新增 Electron 回归 `session-request.spec.ts`，在真实 Chromium + 本地 Cookie 闸门站点验证登录态采集（先经嵌入式浏览器 /login 种下 Profile Cookie）：`useSession` 游标请求携带会话 Cookie 通过闸门完成三页采集，隔离请求未带 Cookie 以 `REQUEST_FAILED` 失败。首轮回归暴露真实缺陷——`net.request({ session })` 不会自动附带会话 Cookie，必须显式 `useSessionCookies: true`，已在 `HttpHost` 修复；全套 Electron 回归 14 通过 1 跳过（真实外网站点用例）。真实外网与发行验收本批未重跑。

2026-09-16 第二十七批：实现"服务与交付"Webhook 可靠投递——持久 outbox。契约层新增 webhook delivery 模型与 Schema；网关新增 `WebhookOutbox`：投递事件（任务终态回调）持久化落盘，独立循环按指数退避重试（含 5xx/网络错误），达到上限放弃并标记，跨重启续投未完成条目；`submit` 接受 webhook 选项，任务进入终态后同一事务内入箱，`close` 等待在途投递完成。修复：outbox 仅在队列 start 时启动，避免残留死投递。

第二十七批验证：`pnpm check` 通过（类型、Lint、构建与架构/i18n 约定）；单元测试覆盖投递成功、失败重试、放弃与跨重启续投（`queue.test.ts` / `debug-outbox.test.ts`）。Webhook 的 Electron 端到端与真实外网验收本批未重跑。

2026-09-16 第二十八批：实现"服务与交付"限流。网关配置新增可选 `rateLimit`（每秒请求数 0–10000，缺省/0 不限流，环境变量 `CLAWLER_GATEWAY_RATE_LIMIT`）；`GatewayServer` 内置固定窗口计数器：认证之后、路由分发之前逐请求计数，超出本秒预算即以 `RATE_LIMITED` 返回 429 并附整数秒 `Retry-After` 头，下一窗口自动恢复；不做请求排队或延迟。同时补上第二十七批遗留的 `WebhookOutboxOptions` 类型导入缺失（`tsc --noEmit` 曾失败）。

第二十八批验证：`pnpm check` 通过（类型、Lint、构建与架构/i18n 约定），19 个测试文件 166 项测试通过；server 新增用例覆盖预算内放行、超出后 429＋`RATE_LIMITED`＋整数秒 `Retry-After`、下一窗口恢复，以及环境变量解析（合法值/0/NaN/负数拒绝）。限流未设置时行为与既有路由完全一致（既有用例未改动即通过）。