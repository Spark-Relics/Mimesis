# Mimesis 研发约束

产品目标是完整的企业级、可编排、便携通用爬虫网关。不得将整体范围自行降为演示或少量站点工具；按依赖分批实现，每批报告实际状态。当前能力和完整验收范围以 docs/engineering-plan.md 为准。

## 产品与设计

- 使用顶部导航、单列任务工作区，不使用左右侧栏后台布局。浏览、编排、运行证据按模式切换。
- 新增或重构交互前，先生成设计图及必要素材，说明状态与操作，再实现。不为纯内部重构重复制作效果图。
- 所有可见功能必须连接真实行为，覆盖空、运行、成功、失败、取消、恢复状态。模型未接入不能显示假 Agent 能力。
- Agent 的生成、修改建议、差异查看、验证、发布必须区分；已发布流程不因自动修复而被悄悄改写。
- 可访问性、中文/英文、最小窗口尺寸和真实 Electron 截图必须纳入交互验收。

## 架构与代码

- 契约与 SDK 向底层依赖；执行核心不依赖 Electron 或 UI；特权行为通过 BrowserHost 与 preload 边界。
- 新模块先说明输入输出、状态归属、失败语义和扩展接口，再更新 scripts/architecture-policy.mjs。禁止循环依赖和跨包相对导入。
- 桌面操作与 API 复用领域能力；不要为同一能力维护两套执行逻辑。
- 不保留废弃功能、影子实现、无调用组件或叠加式样式补丁。历史格式只进入迁移模块，升级前备份真实用户数据。
- 不为未落地的功能预建空实现、通用框架或假接口。当前实现保持短而明确的职责；必要时拆分模块。
- 所有 UI 文案使用 @clawler/i18n。禁止三元表达式，沿用自动检查。
- 普通页面、模型生成源码和调用参数均不获得任意 Node、文件系统或 Electron 权限。

## 闭环验收

- 每轮研发结束前主动提交本轮已完成且通过验证的代码，按独立改动拆分提交；默认不推送。不得把已完成工作长期堆在工作区。提交前检查差异与测试结果，报告提交标识和未完成事项。

- 功能完成须具备配置/输入、真实执行、结果交付、可定位错误、取消/资源释放和必要恢复语义。
- 涉及存储须验证迁移、原子提交、损坏数据、写入失败；不能静默丢失或重归属用户数据。
- 浏览器功能使用可控本地网站作回归，并按需进行真实外网验证。通过示例不等于通用或长期稳定。
- 运行 pnpm check；影响主进程、preload、持久化或执行链路时运行相应 Electron 端到端测试。修复后验证关联路径，不无故反复跑全部测试。
- 交付说明必须区分：已实现、已验证、仍未实现；不得承诺未测量的成功率、并发、上线日期或操作系统机器码隔离。
